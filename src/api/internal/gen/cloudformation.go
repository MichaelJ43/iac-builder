package gen

import (
	"context"
	"fmt"
	"strings"
)

// CloudFormationEmitter MVP: stub minimal template (full implementation in follow-up PR).
type CloudFormationEmitter struct{}

func (CloudFormationEmitter) Preview(_ context.Context, s WizardState) (map[string]string, error) {
	var b strings.Builder
	fmt.Fprintf(&b, `# Generated by iac-builder (AWS CloudFormation) — stub MVP
# Replace Resources with a full EC2 definition or merge follow-up branch.
AWSTemplateFormatVersion: '2010-09-09'
Description: iac-builder stub for %s in %s
Parameters:
  InstanceType:
    Type: String
    Default: %q
Resources:
  Placeholder:
    Type: AWS::CloudFormation::WaitConditionHandle
Outputs:
  Note:
    Value: "Use Terraform emitter for complete EC2 in MVP; CloudFormation emitter upgraded in PR."
`, s.SubnetID, s.Region, s.InstanceType)
	return map[string]string{"template.yaml": b.String()}, nil
}

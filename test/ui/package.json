{
  "name": "iac-builder-playwright",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node ./scripts/ensure-src-ui-deps.mjs",
    "test": "playwright test",
    "test:ci": "CI=true playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1"
  }
}
